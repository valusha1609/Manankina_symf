{% extends 'base.html.twig' %}

{% block title %}Admin panel{% endblock %}

{% block body %}


    <div class="example-wrapper">
        <p>
            <a href="{{  path('app_article_new') }}" class="btn btn-default">Создать статью</a>
        </p>
        <p>
            <a href="{{ path('homepage') }}" class="btn btn-default">Домой</a>
        </p>
    </div>
{% endblock %}
